import React, { useCallback, memo, FC, useRef, useState } from 'react';
import styled from 'styled-components';

export const Chess: FC = memo(function Chess() {
  return (
    <div>
      Chess yeet
    </div>
  );
});